; ─── Sample inventory — replace IPs with those from `terraform output` ─────
; You can also point this at vagrant boxes / a local VM / WSL for a lab.

[managers]
edulms-mgr-01 ansible_host=203.0.113.10 ansible_user=ubuntu

[workers]
edulms-wkr-01 ansible_host=203.0.113.11 ansible_user=ubuntu

[edulms:children]
managers
workers

[edulms:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_private_key_file=~/.ssh/edulms-sre-key.pem
